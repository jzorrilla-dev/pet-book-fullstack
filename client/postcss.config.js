// Asignar a una variable antes de exportar (soluciona el error de ESLint)
const postcssConfig = {
    plugins: {
      tailwindcss: {},
      autoprefixer: {},
    }
  };
  
  export default postcssConfig;